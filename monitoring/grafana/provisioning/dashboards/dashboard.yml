apiVersion: 1

providers:
  - name: 'EDF Dashboards'
    orgId: 1
    folder: 'EDF'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards